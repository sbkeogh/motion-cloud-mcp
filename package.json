{
  "name": "motion-cloud-mcp",
  "version": "1.0.0",
  "description": "Cloud-based Motion.ai MCP server for regular Claude",
  "main": "src/index.ts",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.7.3",
    "wrangler": "^3.98.0"
  }
}